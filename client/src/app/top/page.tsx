import { Button, HStack } from "@chakra-ui/react";

const Page = () => {
  return (
    <HStack>
      <Button>Click me</Button>
      <Button>Click me</Button>
    </HStack>
  );
};

export default Page;
